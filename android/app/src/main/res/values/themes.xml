<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the window before Compose draws; follows the system's light or dark mode. -->
    <style name="Theme.CameraGps" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
